# ── Byte-compiled / optimised / DLL files ─────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class

# ── C extensions ──────────────────────────────────────────────────────────────
*.so

# ── Distribution / packaging ──────────────────────────────────────────────────
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# ── PyInstaller ───────────────────────────────────────────────────────────────
*.manifest
*.spec

# ── Installer logs ────────────────────────────────────────────────────────────
pip-log.txt
pip-delete-this-directory.txt

# ── Unit test / coverage reports ──────────────────────────────────────────────
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# ── Translations ──────────────────────────────────────────────────────────────
*.mo
*.pot

# ── Django ────────────────────────────────────────────────────────────────────
local_settings.py
db.sqlite3
db.sqlite3-journal

# ── Flask ─────────────────────────────────────────────────────────────────────
instance/
.webassets-cache

# ── Scrapy ────────────────────────────────────────────────────────────────────
.scrapy

# ── Sphinx documentation ──────────────────────────────────────────────────────
docs/_build/

# ── PyBuilder ─────────────────────────────────────────────────────────────────
.pybuilder/
target/

# ── Jupyter Notebook ──────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb_checkpoints

# ── IPython ───────────────────────────────────────────────────────────────────
profile_default/
ipython_config.py

# ── pyenv ─────────────────────────────────────────────────────────────────────
.python-version

# ── pipenv ────────────────────────────────────────────────────────────────────
Pipfile.lock

# ── poetry ────────────────────────────────────────────────────────────────────
poetry.lock

# ── pdm ───────────────────────────────────────────────────────────────────────
.pdm.toml

# ── PEP 582 ───────────────────────────────────────────────────────────────────
__pypackages__/

# ── Celery ────────────────────────────────────────────────────────────────────
celerybeat-schedule
celerybeat.pid

# ── SageMath ──────────────────────────────────────────────────────────────────
*.sage.py

# ── Environments ──────────────────────────────────────────────────────────────
.env
.env.*
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# ── Spyder ────────────────────────────────────────────────────────────────────
.spyderproject.cfg
.spyproject

# ── Rope ──────────────────────────────────────────────────────────────────────
.ropeproject

# ── mkdocs ────────────────────────────────────────────────────────────────────
/site

# ── mypy ──────────────────────────────────────────────────────────────────────
.mypy_cache/
.dmypy.json
dmypy.json

# ── Pyre ──────────────────────────────────────────────────────────────────────
.pyre/

# ── pytype ────────────────────────────────────────────────────────────────────
.pytype/

# ── Cython ────────────────────────────────────────────────────────────────────
cython_debug/

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── Project-specific ──────────────────────────────────────────────────────────
# Vendor / downloaded market data (may contain licensed data)
data/*
# Exceptions: v1.3 production NSE index CSVs (from niftyindices.com via nselib)
!data/midcap150_history.csv
!data/momentum30_history.csv
# Exceptions: v2.2 defensive basket runtime artifacts + rebuild source data
!data/defensive_basket_holdings.parquet
!data/defensive_basket_daily_returns.parquet
!data/bse_pipeline/
!data/bse_pipeline/*.parquet
!data/quality_factor/
!data/quality_factor/*.parquet
!data/momentum_scores/
!data/momentum_scores/scored_universe.parquet
!data/momentum_scores/quality_scored_universe.parquet

# Results exports — experimental outputs, regenerated by experiments/ scripts.
# Only the headline summary CSV (produced by make_plots.py) is tracked.
results/*
!results/results_summary.csv

# Images / charts (regenerated by make_plots.py — tracked)
# images/

# Root-level scratch / diagnostic scripts and outputs (work-in-progress that
# hasn't been promoted into experiments/). Anything in experiments/ is tracked.
/diagnose_*.py
/diagnose_*.txt
/research_*.py
/research_*_results.txt
/test_*.py
/test_*_results.txt
/attribution_v14_results.txt
/three_way_yearly_comparison_results.txt
/v14_gap_diagnosis.txt

# README scratch helper (regenerable; not part of the published experiments)
experiments/_readme_data_v21.py

# Temporary lock files (Office / LibreOffice)
.~lock.*
~$*
lu4*.tmp

# Personal artifacts (not part of the project)
Neil_Kapadia_Resume_v*.docx
Neil_Kapadia_Resume_v*.pdf
Recruiting_Tracker.xlsx

# v1.2 diagnostic scripts and outputs (local only)
test_config4_oos.py
oos_2026_check.py
v12_full_output.txt
v12_oos_2026_output.txt

# v1.3 diagnostic scripts and outputs (local only)
generate_v13_numbers.py
pull_nse_history.py
test_v13_oos_and_costs.py
verify_index_data.py
v13_full_output.txt
parked/

# yfinance cache (regenerated on demand by experiments/ scripts)
_yf_cache.pkl
*.pkl

# Local API keys — never commit
experiments/stock_momentum/config_local.py
